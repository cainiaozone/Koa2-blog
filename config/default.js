const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: 'cainiao',
    PORT: '3306',
    HOST: '39.106.8.226'
  }
}

module.exports = config